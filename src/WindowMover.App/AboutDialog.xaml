<Window x:Class="WindowMover.App.AboutDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="About WindowMover"
        Width="380" Height="260"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Background="#1E1E2E"
        ShowInTaskbar="False"
        WindowStyle="ToolWindow">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="WindowMover"
                   Foreground="White" FontSize="22" FontWeight="Bold" />

        <TextBlock Grid.Row="1" x:Name="VersionText"
                   Foreground="#A0A8C0" FontSize="13" Margin="0,4,0,0" />

        <TextBlock Grid.Row="2" Foreground="#A0A8C0" FontSize="13" Margin="0,16,0,0">
            <Run Text="Developed by " />
            <Run Text="Hannah Vernon" Foreground="#7AB0FF" FontWeight="SemiBold" />
        </TextBlock>

        <TextBlock Grid.Row="3" Margin="0,8,0,0">
            <Hyperlink NavigateUri="https://github.com/HannahVernon/WindowMover"
                       RequestNavigate="Hyperlink_RequestNavigate"
                       Foreground="#508CF0" TextDecorations="None">
                github.com/HannahVernon/WindowMover
            </Hyperlink>
        </TextBlock>

        <TextBlock Grid.Row="4" x:Name="CopyrightText"
                   Foreground="#707888" FontSize="11" Margin="0,16,0,0" />

        <Button Grid.Row="6" Content="OK" Click="OK_Click" IsDefault="True"
                HorizontalAlignment="Right"
                Padding="20,8"
                Background="#508CF0" Foreground="White"
                BorderBrush="#6EA0FF" Cursor="Hand" />
    </Grid>
</Window>
