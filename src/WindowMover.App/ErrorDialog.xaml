<Window x:Class="WindowMover.App.ErrorDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="WindowMover Error"
        Width="440" Height="200"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        Background="#1E1E2E">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0"
                   Text="{Binding Message}"
                   Foreground="#E0E0E0" FontSize="14"
                   TextWrapping="Wrap" VerticalAlignment="Center" />

        <StackPanel Grid.Row="1" Orientation="Horizontal"
                    HorizontalAlignment="Right" Margin="0,16,0,0">
            <Button Content="View Log" Click="ViewLog_Click"
                    Padding="16,8" Margin="0,0,10,0"
                    Background="#3A3A5A" Foreground="#E0E0E0"
                    BorderBrush="#5A5A8A" Cursor="Hand" />
            <Button Content="OK" Click="OK_Click" IsDefault="True"
                    Padding="16,8"
                    Background="#508CF0" Foreground="White"
                    BorderBrush="#6EA0FF" Cursor="Hand" />
        </StackPanel>
    </Grid>
</Window>
